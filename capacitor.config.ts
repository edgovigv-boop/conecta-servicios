const config = {
  appId: 'com.conectaservicios.app',
  appName: 'Conecta Servicios',
  webDir: 'out',
  server: {
    androidScheme: 'https',
    cleartext: false,
  },
  android: {
    allowMixedContent: false,
    webContentsDebuggingEnabled: false,
    captureInput: true,
  },
};

export default config;
